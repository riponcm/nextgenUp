flask>=3.0
# Server-side AI upscaling (optional but recommended)
onnxruntime>=1.20
pillow>=10.0
numpy>=1.26
# Face restoration (optional; also needs the GFPGAN model — see setup.sh)
# opencv-python-headless>=4.10
